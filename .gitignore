# ── Secrets — never commit ──
.env
config/user.json

# ── Personal data — tracked in private fork (career-os-sp) ──
# config/user.json is still gitignored (contains no secrets here but kept consistent)
# Resumes, job tracker, market intel, checkpoints — all versioned privately below

# Only keep the .gitkeep markers out of the public repo ignore:
# (these lines are intentionally removed for private use)

# ── Dependencies ──
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# ── Puppeteer / Playwright cache ──
.local-chromium/
.playwright-cache/

# ── OS files ──
.DS_Store
Thumbs.db

# ── Temp files ──
*.tmp
*.temp
